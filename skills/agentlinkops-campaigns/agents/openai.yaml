interface:
  display_name: AgentLinkOps Campaigns
  short_description: Plan and qualify backlink campaigns for a product using guest posts, resource additions, broken-link replacement, compet
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: mcp
      value: agentlinkops
      description: AgentLinkOps hosted MCP server (verified backlink observations, monitoring and evidence).
      transport: streamable_http
      url: https://app.agentlinkops.com/mcp
